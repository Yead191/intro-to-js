function double(n) {
    const db = n*2
    console.log('double number of', n, 'is:', db);
    
}

// double(50)

// const money = 120
// double(money)


function difference(x,y) {
    sub = x-y
    console.log('sub of', x, 'and', y, 'is:', sub);
    
}
difference(100,57)

const fatherAge = 54
const myAge  = 23

difference(fatherAge, myAge)

